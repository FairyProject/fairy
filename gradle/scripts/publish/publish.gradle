apply plugin: "maven-publish"

project.afterEvaluate {
    // Gradle Plugin already contains a publication
    if (project.name != "gradle-plugin") {
        apply from: "${rootDir}/gradle/scripts/publish/publication.gradle"
    }
    apply from: "${rootDir}/gradle/scripts/publish/repository.gradle"
}


